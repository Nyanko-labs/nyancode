name: dev
description: Start development workspace with tmux

steps:
  - name: start_tmux
    command: tmux new-session -d -s nyan-dev
    description: Create tmux dev session

  - name: open_nvim
    command: tmux send-keys -t nyan-dev "nvim" C-m
    description: Open nvim in first pane

  - name: open_ai
    command: tmux split-window -h -t nyan-dev && tmux send-keys -t nyan-dev.1 "opencode" C-m
    description: Open OpenCode in second pane

  - name: dev_server
    command: tmux split-window -v -t nyan-dev.1 && tmux send-keys -t nyan-dev.2 "npm run dev" C-m
    description: Start dev server in third pane

  - name: test_runner
    command: tmux split-window -v -t nyan-dev.0 && tmux send-keys -t nyan-dev.3 "npm test --watch" C-m
    description: Start test watcher in fourth pane

ai_usage_points:
  - trigger: code_generation
    tool: opencode generate
  - trigger: refactoring
    tool: opencode refactor