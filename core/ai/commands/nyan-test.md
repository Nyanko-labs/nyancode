---
description: Write tests for the given code
---
Write tests for the following code using the test framework this project already uses (check package.json, pyproject, Cargo.toml, go.mod). Cover the happy path, one edge case, and one failure case. Output only the test file in a fenced block, with its path as the first line comment.

$ARGUMENTS
