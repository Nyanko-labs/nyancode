---
description: Write a conventional commit message for staged changes
agent: plan
---
Write a conventional commit message (type(scope): subject, blank line, short body) for these staged changes. Output only the message, no fences, no commentary.

!`git diff --cached`
